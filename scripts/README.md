Running `./gen_versioned_apidump.sh` as of now (october 2025), generates 1.4 GiB of API Dumps all the way from 2017 to latest commit of Roblox-Client-Tracker.
You need to `git submodules update --init --recursive` before doing this, unless you cloned with `--recursive`.
